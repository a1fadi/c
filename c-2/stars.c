// a program that allows the user to enter outputs and create a box of stars as big as they desire
// 15/06/2021
// fadi al hatu

#include <stdio.h>
int main(void) { 
    int size;

    printf("Enter size: ");
    scanf("%d", &size);
    
    
    int sizey = 0;
    // while size counter is smaller than inputed size
    while (sizey < size) { 
        if
    
    
    
    /*
    if (sizey <size) {
        int sizex = 0;
        while (sizex < size) {
        printf("*");  
        sizex++; }
        printf("\n");
        sizey++; }
        }*/
        
/*
        int sizey=0;
        if (sizey < size) {
        printf("\n");  } 
        sizey++; }
  */      
        return 0;
        }
